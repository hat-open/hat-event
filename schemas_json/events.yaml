$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "hat-event://events.yaml"
definitions:
    events:
        engine:
            enum:
                - STARTED
                - STOPPED
        eventer:
            enum:
                - CONNECTED
                - DISCONNECTED
        synced:
            type: boolean
