---
"$schema": "http://json-schema.org/schema#"
id: "hat-event://events.yaml#"
definitions:
    events:
        engine:
            enum:
                - STARTED
                - STOPPED
        eventer:
            enum:
                - CONNECTED
                - DISCONNECTED
        synced:
            type: boolean
...
